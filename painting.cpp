#include<bits/stdc++.h>
using namespace std;
using ll=long long;
int main()
{
    ll t;
    cin>>t;
    while(t--)
    {
        ll n;
        vector<ll> vec(n);
        set<ll> s;
        for(ll& x : vec)
        {
            cin>>x;
            s.insert(x);
        }
        
    }
}