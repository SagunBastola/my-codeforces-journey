#include<bits/stdc++.h>
using namespace std;
using ll =long long;
int main()
{
    ll t;
    cin>>t;
    while(t--)
    {
        ll n,x;
        cin>>n>>x;
        vector<vector<int>> vec(n);
        for(vector<int>& x : vec)
        {
            int a,b,c;
            cin>>a>>b>>c;
            x.push_back(a);
            x.push_back(b);
            x.push_back(c);
        }
        int k=0;
        for
    }
}