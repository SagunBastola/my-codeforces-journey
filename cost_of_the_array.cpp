#include<bits/stdc++.h>
using namespace std;
using ll=long long ;
#define rep(i,n,m) for(int i=n;i<m;i++)
int main()
{
    ll t;
    cin>>t;
    while(t--)
    {
        ll n,k;
        cin>>n>>k;
        vector<int> vec(n);
        rep(i,0,n)
        {
            cin>>vec[i];
        }
    }
}